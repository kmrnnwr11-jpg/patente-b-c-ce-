rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Cache audio per parola
    match /audio/words/{lang}/{file} {
      allow read, write: if request.auth != null;
    }
  }
}

